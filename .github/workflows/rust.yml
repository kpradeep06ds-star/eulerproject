name: Rust

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: p33

    steps:
      - uses: actions/checkout@v4

      - name: Check compilation
        run: cargo check
